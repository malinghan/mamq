# v2.0 — 持久化存储，重启不丢

## 1. 版本目标

把 v1.0 的内存 `ArrayList` 替换为基于 `MappedByteBuffer` 的文件存储，加 `Indexer` 内存索引加速读取。

重启服务后消息不丢失，消费进度不丢失。理解消息系统如何做持久化。

**只改存储层，其他不动**：HTTP 接口、模型类、订阅逻辑与 v1.0 完全一致，新增 `/batch` 接口。

---

## 2. 核心功能说明

### 新增两个组件

**Store**：消息的持久化读写，底层是 `MappedByteBuffer`（内存映射文件）。
- 写入：把 Message 序列化为 JSON，加 10 字节长度头，追加写入 `.dat` 文件
- 读取：根据 Indexer 提供的 `(position, length)` 直接定位读取
- 启动时：扫描 `.dat` 文件重建 Indexer

**Indexer**：内存中的消息索引，加速随机读取。
- 维护 `offset → Entry{position, length}` 映射
- `offset` 是消息序号（0, 1, 2...），`position` 是消息在文件中的字节起始位置

### 消息存储格式

```
.dat 文件布局（每条消息紧密排列）：

┌──────────────┬──────────────────────────────────┐
│  10字节长度头  │  N字节 JSON 消息体                │
│  "0000000042" │  {"id":1,"body":"hello",...}      │
└──────────────┴──────────────────────────────────┘

示例（3条消息）：
位置 0:   "0000000042{"id":1,"body":"hello","headers":{}}"
位置 52:  "0000000038{"id":2,"body":"world","headers":{}}"
位置 90:  "0000000041{"id":3,"body":"mamq","headers":{}}"
```

### Indexer 内存结构

```
offset → Entry{ position, length }
  0  →  { position=0,   length=42 }
  1  →  { position=52,  length=38 }
  2  →  { position=90,  length=41 }
```

读取 offset=1 的消息：
1. `Indexer.getEntry(1)` → `{position=52, length=38}`
2. `buffer.get(52, 38)` → 读出 38 字节
3. JSON 反序列化 → `Message`

### 为什么用 MappedByteBuffer

普通 FileOutputStream 每次写入都有系统调用开销。`MappedByteBuffer` 把文件映射到内存地址空间，读写像操作内存数组一样，OS 负责异步刷盘，兼顾性能和持久化。

### 新增 /batch 接口

`GET /mamq/batch?t={topic}&cid={consumerId}&size={n}`

一次拉取最多 n 条消息，从当前 offset 开始，返回消息列表。不自动 ACK，消费者需逐条或批量 ACK。

---

## 3. 代码结构

```
src/main/java/cn/malinghan/mamq/
├── model/
│   ├── Message.java          # 不变
│   ├── Subscription.java     # 不变
│   ├── Stat.java             # 不变
│   └── Result.java           # 不变
├── store/
│   ├── Store.java            # 新增：MappedByteBuffer 文件读写
│   └── Indexer.java          # 新增：内存索引 offset → (position, length)
├── server/
│   ├── MessageQueue.java     # 改动：messages 列表换成 Store
│   └── MQServer.java         # 改动：新增 /batch 接口
└── MamqApplication.java      # 不变
```

### 层次关系

```
MQServer（HTTP层，不变）
    │
    ▼
MessageQueue（核心层）
    ├── subscriptions: ConcurrentHashMap   ← 不变
    └── store: Store                       ← 替换原来的 ArrayList
          ├── MappedByteBuffer (.dat 文件)
          └── Indexer（内存索引）
                └── Map<Integer, Entry>
```

### 文件存储位置

```
{工作目录}/mamq-data/
  └── {topic}.dat      ← 消息数据文件
```

---

## 4. 代码实现说明

### Indexer.java

```java
public class Indexer {

    @Data
    @AllArgsConstructor
    public static class Entry {
        private int position;  // 消息在文件中的字节起始位置
        private int length;    // 消息体字节长度（不含10字节头）
    }

    // offset（消息序号）→ Entry
    private final Map<Integer, Entry> index = new HashMap<>();
    private int nextOffset = 0;

    public void addEntry(int position, int length) {
        index.put(nextOffset++, new Entry(position, length));
    }

    public Entry getEntry(int offset) {
        return index.get(offset);
    }

    public int size() {
        return nextOffset;
    }
}
```

### Store.java

```java
public class Store {

    private static final int HEADER_LEN = 10;  // 长度头固定10字节
    private static final int FILE_SIZE = 1024 * 1024 * 10;  // 10MB，可调整

    private final MappedByteBuffer buffer;
    private final Indexer indexer = new Indexer();
    private int writePosition = 0;  // 当前写入位置

    public Store(String topic) {
        try {
            File dir = new File("mamq-data");
            dir.mkdirs();
            File file = new File(dir, topic + ".dat");
            RandomAccessFile raf = new RandomAccessFile(file, "rw");
            raf.setLength(FILE_SIZE);
            this.buffer = raf.getChannel().map(
                FileChannel.MapMode.READ_WRITE, 0, FILE_SIZE);
            // 启动时扫描文件，重建索引
            init();
        } catch (IOException e) {
            throw new RuntimeException("Store init failed", e);
        }
    }

    // 启动时扫描 .dat 文件，重建 Indexer
    private void init() {
        buffer.position(0);
        while (buffer.position() < FILE_SIZE) {
            // 读 10 字节长度头
            byte[] header = new byte[HEADER_LEN];
            buffer.get(header);
            String lenStr = new String(header).trim();
            if (lenStr.isEmpty()) break;  // 到达未写入区域

            int length = Integer.parseInt(lenStr);
            int position = buffer.position();  // 消息体起始位置
            indexer.addEntry(position, length);
            buffer.position(position + length);  // 跳过消息体
            writePosition = buffer.position();
        }
    }

    // 写入消息，返回 offset
    public synchronized int write(Message message) {
        String json = JSON.toJSONString(message);
        byte[] body = json.getBytes(StandardCharsets.UTF_8);
        // 10字节长度头，左补零
        String header = String.format("%010d", body.length);
        byte[] headerBytes = header.getBytes(StandardCharsets.UTF_8);

        int msgPosition = writePosition + HEADER_LEN;  // 消息体起始位置
        buffer.position(writePosition);
        buffer.put(headerBytes);
        buffer.put(body);
        writePosition = buffer.position();

        indexer.addEntry(msgPosition, body.length);
        return indexer.size() - 1;  // 返回 offset
    }

    // 按 offset 读取消息
    public Message read(int offset) {
        Indexer.Entry entry = indexer.getEntry(offset);
        if (entry == null) return null;

        byte[] body = new byte[entry.getLength()];
        buffer.position(entry.getPosition());
        buffer.get(body);
        return JSON.parseObject(new String(body, StandardCharsets.UTF_8), Message.class);
    }

    public int size() {
        return indexer.size();
    }
}
```

### MessageQueue.java（改动部分）

```java
public class MessageQueue {
    private final String topic;
    private final Store store;                                          // ← 替换 ArrayList
    private final Map<String, Subscription> subscriptions = new ConcurrentHashMap<>();

    public MessageQueue(String topic) {
        this.topic = topic;
        this.store = new Store(topic);                                  // ← 初始化 Store
    }

    public int send(Message message) {
        return store.write(message);                                    // ← 写文件
    }

    public Message recv(String consumerId) {
        Subscription sub = subscriptions.get(consumerId);
        if (sub == null || sub.getOffset() >= store.size()) return null;
        return store.read(sub.getOffset());                             // ← 读文件
    }

    // 批量消费，最多返回 size 条
    public List<Message> batch(String consumerId, int size) {
        Subscription sub = subscriptions.get(consumerId);
        if (sub == null) return Collections.emptyList();
        List<Message> result = new ArrayList<>();
        int offset = sub.getOffset();
        for (int i = 0; i < size; i++) {
            Message msg = store.read(offset + i);
            if (msg == null) break;
            result.add(msg);
        }
        return result;
    }

    public int ack(String consumerId, int offset) {
        Subscription sub = subscriptions.get(consumerId);
        if (sub != null) sub.setOffset(offset + 1);
        return offset;
    }

    public Stat stat(String consumerId) {
        Subscription sub = subscriptions.get(consumerId);
        int position = sub == null ? 0 : sub.getOffset();
        return new Stat(store.size(), position);
    }

    // subscribe / unsubscribe 与 v1.0 完全一致，略
}
```

### MQServer.java（新增 /batch）

```java
@GetMapping("/batch")
public Result<List<Message>> batch(@RequestParam String t,
                                   @RequestParam String cid,
                                   @RequestParam(defaultValue = "10") int size) {
    List<Message> msgs = getOrCreate(t).batch(cid, size);
    return Result.ok(msgs);
}
```

---

## 5. 测试流程

启动服务：

```bash
mvn spring-boot:run
```

### 场景一：基础持久化验证

```bash
# Step 1: 发 5 条消息
for i in 1 2 3 4 5; do
  curl -s -X POST "http://localhost:8765/mamq/send?t=test.t2" \
    -H "Content-Type: application/json" \
    -d "{\"id\":$i,\"body\":\"msg-$i\",\"headers\":{}}"
  echo ""
done
# 期望每条返回: {"code":1,"data":0} {"code":1,"data":1} ... {"code":1,"data":4}

# Step 2: 订阅，消费前 3 条并 ACK
curl -s "http://localhost:8765/mamq/sub?t=test.t2&cid=c1"

for offset in 0 1 2; do
  echo "=== recv offset=$offset ==="
  curl -s "http://localhost:8765/mamq/recv?t=test.t2&cid=c1"
  echo ""
  curl -s "http://localhost:8765/mamq/ack?t=test.t2&cid=c1&offset=$offset"
  echo ""
done
# 期望: 依次拿到 msg-1, msg-2, msg-3

# Step 3: 查看当前进度
curl -s "http://localhost:8765/mamq/stat?t=test.t2&cid=c1"
# 期望: {"code":1,"data":{"total":5,"position":3}}
```

### 场景二：重启恢复验证

```bash
# Step 4: 停止服务（Ctrl+C），重新启动
mvn spring-boot:run

# Step 5: 重启后验证消息仍在
curl -s "http://localhost:8765/mamq/stat?t=test.t2&cid=c1"
# 期望: {"code":1,"data":{"total":5,"position":0}}
# 注意: total=5 说明消息持久化成功；position=0 是因为 Subscription 在内存中，重启后重置
# （消费位点持久化是 v4.0 的工作）

# Step 6: 重新订阅，继续消费
curl -s "http://localhost:8765/mamq/sub?t=test.t2&cid=c1"
curl -s "http://localhost:8765/mamq/recv?t=test.t2&cid=c1"
# 期望: 拿到 msg-1（从头开始，因为 offset 重置了）
```

### 场景三：batch 批量消费

```bash
# 重新订阅（或使用新 cid）
curl -s "http://localhost:8765/mamq/sub?t=test.t2&cid=c2"

# 一次拉取 3 条
curl -s "http://localhost:8765/mamq/batch?t=test.t2&cid=c2&size=3"
# 期望: {"code":1,"data":[{"id":1,...},{"id":2,...},{"id":3,...}]}

# batch 不自动 ACK，手动 ACK 最后一条（推进到 offset=3）
curl -s "http://localhost:8765/mamq/ack?t=test.t2&cid=c2&offset=2"

# 再 batch，拿剩余 2 条
curl -s "http://localhost:8765/mamq/batch?t=test.t2&cid=c2&size=10"
# 期望: {"code":1,"data":[{"id":4,...},{"id":5,...}]}
```

### 场景四：验证文件已生成

```bash
# 查看数据文件
ls -la mamq-data/
# 期望: 看到 test.t2.dat 文件

# 查看文件内容（前 200 字节）
head -c 200 mamq-data/test.t2.dat
# 期望: 看到 "0000000..." 开头的长度头 + JSON 消息体
```

---

## 6. 版本总结

### 学到了什么

v2.0 展示了消息系统持久化的核心思路：

```
写入：Message → JSON → [10字节长度头 + JSON字节] → 追加写入 .dat 文件
                                                          ↓
                                                   Indexer 记录位置

读取：offset → Indexer.getEntry() → (position, length) → 直接定位读取
```

- **为什么要长度头**：文件是字节流，没有分隔符，长度头告诉读取方"这条消息有多少字节"
- **为什么要 Indexer**：没有索引就只能从头扫描，有了索引可以 O(1) 定位任意消息
- **MappedByteBuffer 的好处**：读写像操作内存，OS 负责异步刷盘，不需要手动 flush

### 与 v1.0 的对比

| 方面 | v1.0 | v2.0 |
|------|------|------|
| 消息存储 | `ArrayList`（内存） | `MappedByteBuffer`（文件） |
| 重启后消息 | 全部丢失 | 完整保留 |
| 读取方式 | `list.get(offset)` | `Indexer` 定位 + `buffer.get()` |
| 启动时间 | 即时 | 需扫描文件重建索引 |

### 这个版本的局限

| 问题 | 现象 | 下一版解决 |
|------|------|-----------|
| 消费位点不持久化 | 重启后 offset 归零，需重新消费 | v4.0 `.sub` 文件 |
| 单文件有大小上限 | 文件写满后无法继续写入 | v4.0 分段存储 |
| 无客户端封装 | 调用方仍需手写 HTTP | v3.0 客户端封装 |
